apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: secrets
  namespace: argo
spec:
  generators:
  - list:
      elements:
        - cluster: practice-cluster
          app-name: prometheus
          cluster-name: practice-cluster
          namespace: monitoring
        - cluster: local-cluster
          app-name: prometheus
          namespace: monitoring
          cluster-name: in-cluster
        - cluster: home-cluster
          app-name: prometheus
          cluster-name: default
          namespace: monitoring
  template:
    metadata:
      name: '{{app-name}}-{{cluster-name}}external-secret'
      namespace: argo
    spec:
      project: default
      destination:
        namespace: '{{namespace}}'
        name: '{{cluster-name}}'
      source:
        repoURL: https://github.com/rk280392/my-lab-setup.git
        path: 'applications/{{cluster}}/{{app-name}}/externalSecret'
        targetRevision: HEAD
      syncPolicy:
        syncOptions:
          - ServerSideApply=true
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - Replace=true
        automated:
          prune: true
          selfHeal: true
